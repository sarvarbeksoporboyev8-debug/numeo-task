    // FIX: use functional updater to avoid stale closure over translations
    // Previously: setTranslations([data, ...translations])
    // The old version captured translations from the initial render,
    // so only the latest translation would ever appear.
    socket.on('translation', (data: Translation) => {
      console.log('Translation received:', data);
      setTranslations(prev => [data, ...prev]);
      setIsLoading(false);
    });
